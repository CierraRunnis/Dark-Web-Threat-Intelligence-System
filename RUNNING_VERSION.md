# 当前运行版本源码快照

本分支保存 2026 年 9 月 5 日核对的运行项目源码，包含当时已生效但尚未提交的功能。它不是原开发仓库 5 月 15 日的初始提交。

## 版本范围

- 后端对应当前 API 和采集服务使用的源码目录；核对时，后端应用源码没有在服务启动后被修改。
- 前端对应当前 Vite preview 服务提供的构建，包含 AI 聚合轮询修复及 Word 原生格式导出。
- 保留应用源码、前端静态资源、依赖锁文件、启动脚本、测试及相关项目文档。

## 排除内容

不包含实际 `.env`、API 密钥、数据库、账户和会话数据、采集结果、日志、已安装依赖、生成报告、本机备份、Tor 迁移备份包或规划草稿。前端 `dist` 可由源码和依赖锁文件重新构建，不随源码提交。

API 连通性测试中的硬编码密钥已替换为环境变量 `RANSOMWARE_LIVE_API_KEY`，此改动仅应用于发布快照。`.gitignore` 补充了私有部署文件和备份排除规则，`.env.example` 仅包含空凭据和本机服务的非敏感示例。

## 发布验证

- 前端 47 项测试通过，后端 AI 聚合、HTTP Basic Auth 和账户相关的 25 项测试通过。
- 在独立快照中重新构建，51 个前端发布文件的 SHA-256 均与当前服务提供的构建文件一致。
- 除密钥测试脚本和发布排除规则外，复制的源文件哈希与当前项目目录保持一致。
- 未打包实际私有配置、运行数据、备份文件或已知部署密钥。目标仓库原有第三方声明作为归属信息予以保留。

## 部署准备

先安装 Python 和 Node.js 依赖，为目标机器重新配置 `.env`、数据库和外部服务凭据。不要将示例配置视为已经可用的外部服务连接。

前端构建与运行命令：

```bash
cd threat-intelligence-dashboard
npm ci
npm run build
npm run preview -- --host 0.0.0.0 --port 5173 --strictPort
```

后端启动入口为 `darkweb_collector/scripts/serve_api.py`。现有整套 WSL 启动入口是 `darkweb_collector/scripts/start_all_services_wsl.sh`，该脚本还会管理数据库、采集队列和依赖安装，使用前应按目标环境检查其配置。
