# 更新记录

## v20260831 - 2026-08-31

发布分支：`main`

### 监测对象级通知隔离

- 每个监测对象独立保存情报关键词、企业微信 Bot、钉钉 Webhook及通道启停状态，不再共享机器人凭据和推送目标。
- 勒索、数据泄露、漏洞预警及代码泄露主列表通知先按监测对象匹配，再只发送到该对象已启用的通道；对象间发送记录和去重签名相互隔离。
- 企业微信支持多个对象分别维护监听连接和登记会话；删除对象时同步停止监听并清理该对象通知配置，不影响其他对象。

### 企业画像与代码检索优化

- 企业画像中的企业名称、品牌别名、英文简称、根域名和内部系统关键词自动生成代码搜索词，并按词值去重。
- 原“代码检索词”调整为“附加代码检索词”，仅用于补充项目、产品和特殊搜索词；页面展示企业画像自动生成词供核对。
- 代码敏感规则、扫描策略、情报关键词和通知通道均明确显示当前监测对象；未选择对象时自动禁用对象级配置控件。

### 验证

- 双对象关键词、企业微信、钉钉及代码泄露通知路由隔离回归通过，未配置对象不会回退使用全局环境凭据。
- 隔离 SQLite 与真实 PostgreSQL 写入、企业画像派生词去重、对象删除清理和新 UI 对象切换验证通过。
- Python 编译、PowerShell解析、Windows更新器回归、前端生产构建和更新包敏感文件扫描通过。

## v20260829 - 2026-08-29

发布分支：`main`

### 代码监测质量与通知

- 主列表收紧为真实凭据、私钥、非本地连接串或明确企业系统访问证据；企业名称、公开域名、邮箱、年报、招聘、数据集、报告和演示资料统一进入被压制线索。
- 历史命中按新版分类规则在读取时重新判定；时间筛选改为服务器当前时间，搜索、监测对象、平台、风险、层级和时间条件同时覆盖主列表与被压制列表。
- 代码泄露通知只发送本轮新增且未压制的敏感主列表命中；企业微信与钉钉不再推送普通关键词线索，其他情报关键词通知保持不变。
- 长期扫描完成后自动刷新趋势、统计和两个列表，并区分新增主列表、新增压制、更新已有、敏感命中与普通线索。

### 站点采集修复

- DarkForums 迁移至 `darkforums.as`，列表与详情优先直连，失败时回退浏览器抓取。
- Cracked 的列表与详情统一交由 `browser_render` worker 消费；直连或代理返回 403 后自动回退 Chromium，无需登录。
- Cracked 使用隔离的匿名浏览器状态保存反爬挑战结果，状态位于数据目录，不进入更新包、仓库或登录 Cookie 管理。
- 临时 HTML 与 Cookie 文件按请求隔离，403 归类为站点拦截，Windows 控制台特殊字符不会再中断任务。

### 验证

- 固定真阳性与误报回归、通知去重、双列表分页筛选、SQLite兼容及真实 PostgreSQL 写入验证通过。
- Cracked 在强制直连与代理均返回 403 的条件下，真实 Chromium 成功解析列表和详情并生成截图；DarkForums 真实列表与详情低频冒烟通过。
- Python 编译、PowerShell 解析、Windows 更新器回归、前端生产构建和发布包凭据扫描通过。

## v20260828 - 2026-08-28

发布分支：`main`

### 监控关键词双通道通知

- 勒索情报、数据泄露和漏洞预警的监控关键词命中可同时推送至企业微信与钉钉，两个通道独立记录发送状态，单一通道失败不会阻断另一通道。
- 默认只处理本轮新入库事件；保存机器人配置、修改关键词或重启服务均不会补发历史命中，避免重复推送形成信息轰炸。
- 通知内容包含事件标题、来源、受害对象、关联主体、披露时间、摘要和来源链接，不发送代码片段或凭据内容。

### 新 UI 长期代码扫描

- 在实际渲染的代码监测主页新增长期扫描控制栏，可选择监测对象并查看运行状态、最近完成时间以及敏感命中和线索命中数量。
- “开始扫描”会立即执行首轮，此后默认每 1 小时自动扫描；同一位置可以停止长期扫描，不再要求进入隐藏的旧扫描页面。
- 企业微信和钉钉仍只推送新增且进入代码泄露监测主列表的记录，不推送被压制线索。

### 验证

- 企业微信/钉钉渠道隔离、新增事件筛选、重复跳过、失败隔离重试和历史企业微信兼容检查通过。
- 新 UI 长期扫描绑定检查、Python 编译和前端生产构建通过；真实浏览器验证开始、停止接口均返回成功且页面状态正确切换。

## v20260827.2.0 - 2026-08-27

发布分支：`main`

### Windows 在线更新修复

- 修复混用 PowerShell 7 与 Windows PowerShell 5.1 时 `Get-FileHash` 模块不可用，导致更新停在“准备新版运行环境”的问题。
- 修复停止旧服务时误杀 `run_self_update.py` 更新控制器，导致页面长期停在“正在停止旧版服务”的问题。
- 运行端口状态改为无 BOM UTF-8，更新器同时兼容带 BOM 文件，避免新版服务实际健康却被误判失败并回滚。
- 更新进程状态检查会确认 Windows 进程仍处于活动状态，不再把已退出但句柄尚存的进程误判为运行中。
- Release 新增 `repair-v20260827-update.ps1` 过渡修复工具，供现有 `v20260827` 系列安装解除卡死状态后重新更新。

## v20260827.1.0 - 2026-08-27

发布分支：`main`

### 通知机器人独立删除

- 企业微信 Bot 与钉钉机器人各自提供独立删除配置按钮和接口；删除任一机器人不会影响另一通道、监测对象、企业画像或历史数据。
- 删除企业微信配置时同步停止智能机器人监听并清除已登记会话；环境变量配置仍然存在时，界面会明确提示其继续生效。
- 新增独立删除专项检查，覆盖配置文件隔离、重复删除幂等、环境变量回退以及前端按钮与 API 路由绑定。

## v20260827 - 2026-08-27

发布分支：`main`

### 无 Git 一键更新

- Windows 版本检查改为读取稳定 Release 清单，点击更新按钮即可完成下载、大小与 SHA-256 校验、安全解压、依赖预安装、服务切换和健康检查，不再要求 Git 或 `.git` 工作副本。
- 新版本安装到独立应用根，通过控制根中的 `installation.json` 原子切换；新版启动或健康检查失败时自动恢复旧版本。
- 更新状态保存在固定控制根，应用版本和下载包写入 `DARKWEB_APP_ROOT`，站点配置、Cookie、SQLite 备份及业务数据写入 `DARKWEB_DATA_ROOT`。
- 新增确定性 Release 更新包构建和发布流水线；可选 Ed25519 清单签名，生产环境可配置为拒绝未签名清单。

### Windows 可配置数据盘

- Windows 普通安装收口为单一 `darkweb.cmd` 入口；首次双击会列出本机磁盘和剩余空间，选择业务数据及后续更新版本的存储盘后，自动完成新环境配置或已有受管理数据的校验迁移，后续启动不再重复询问。
- 新增 `configure-data-root.cmd`，支持新环境预检与配置，以及已有数据的停机复制、逐文件 SHA-256 校验、配置切换、健康检查和失败回滚；源目录默认保留，复测后可用 `cleanup` 再次核验并只清理已确认一致的旧数据。
- 新安装环境的 SQLite、迁移批次、证据镜像、Garnet 检查点、Tor、Playwright、认证文件和缓存统一跟随 `DARKWEB_DATA_ROOT`；C 盘仅保留控制状态和命令入口，应用版本使用独立 `DARKWEB_APP_ROOT`。
- Windows 新装 PostgreSQL 通过 EDB `--datadir` 把数据库集群创建到数据根目录；既有 PostgreSQL 服务只复用并明确显示实际集群路径，不执行不安全的在线文件复制。
- 启动前检查数据盘可用空间，`darkweb status` 与“配置中心 → 数据迁移”显示数据根目录、剩余容量及仍位于外部目录的数据组件。

### 钉钉代码泄露通知

- 新增钉钉代码泄露通知配置、连接测试和发送通道，并与其他通知通道隔离；单一通道失败不会阻断其他通道。
- 前端配置页面展示钉钉状态，敏感凭据不回显。

### 验证

- 无 Git 更新器原有 9 项回归和三根目录 3 项路径测试通过；清空 Git 所在 PATH 后，同版本按钮检查正常完成。
- Windows 隔离目录完成数据根迁移、逐文件校验、字段级安装指针重写、受控 cleanup、外部路径保留和冲突目标失败恢复。
- 数据迁移与更新器跨进程维护锁实测互斥；PowerShell 5.1/7 解析、Python 编译、API 路径摘要和前端生产构建通过。
- 发布候选仍需在合入 `main` 后执行确定性打包、远端资产和服务器灰度验证。

## v20260825 - 2026-08-25

发布分支：`main`

### Windows 原生 Garnet 运行时

- Windows 一键启动不再把 Memurai Developer 作为缺省 Redis 后端；缺少可用服务时自动安装并启动项目托管的 Microsoft Garnet 2.1.4。
- 固定下载微软官方 Windows x64 ReadyToRun 资产和项目私有 .NET 10.0.11 运行时，分别执行 SHA-256 与 SHA-512 校验，不依赖 `winget`、Docker、Git、SDK 或系统级 .NET 安装。
- Garnet 仅监听 `127.0.0.1:6380`，固定使用 Redis DB 0；启用 AOF、提交等待、检查点和恢复，并默认每 6 小时执行一次后台检查点，数据保存在用户数据目录，普通停止和保留数据卸载不会删除检查点。
- 启动器通过 RESP `PING` 判断服务是否真正兼容 Redis，不再把普通 TCP 端口占用误判为可用服务；PID 丢失时也只识别本项目固定运行时及检查点目录对应的 Garnet 进程。
- 兼容旧版写入的 `redis://127.0.0.1:6379/0`：该端点可用时继续复用，不可用时迁移到托管 Garnet；其他显式 `REDIS_URL` 保持用户配置权威，连接失败时不会被静默覆盖。
- Garnet 不作为完整 Redis 替代：当前生产路径已验证 Celery broker/result、`SET NX EX` 状态锁、列表、事务和发布订阅；不得引入 Redis Streams 或非 DB 0 依赖，除非重新进行兼容测试。

### Windows Server PostgreSQL 安装兼容

- Windows 首次准备 PostgreSQL 16 时不再依赖 `winget` 或 Microsoft App Installer；缺少本机服务时直接下载 EDB 官方 PostgreSQL 16.15-1 x64 安装程序，校验固定 SHA-256 后静默安装服务和命令行工具。
- 已安装 PostgreSQL 和显式外部迁移目标继续优先复用；离线环境可通过 `DARKWEB_POSTGRESQL_INSTALLER_PATH` 提供同一官方安装程序，哈希不一致时拒绝执行。

### 站点采集与前端

- 新增 Cracked 与 PwnedForums 列表、详情解析和采集适配器，接入站点注册、标准化刷新和来源名称展示。
- PwnedForums 支持从环境变量或受 Git 忽略的本地文件读取会话 Cookie，并通过 Tor/HTTP 抓取层安全透传。
- 采集控制台新增登录态站点 Cookie 管理，支持脱敏查看、保存、刷新和清除文件 Cookie；Cracked 默认启用，PwnedForums 默认停用。

### 版本与更新

- 当前版本改用日期编号 `v20260825`，后续版本继续使用 `vYYYYMMDD`；历史版本记录保持不变。
- 在线更新默认跟踪 `Threat-Intelligence-monitor/Dark-Web-Threat-Intelligence-System` 的 `main` 分支，可由环境变量显式覆盖；仍以 Git 提交和安全 fast-forward 判断是否可更新，不依赖语义版本比较。

### 验证

- Microsoft Garnet 官方 v2.1.4 发布资产通过 Redis 协议、项目 `RedisStateStore`、10/100 任务 Celery 往返、结果后端、控制 ping、定时 BGSAVE、AOF 强制终止恢复、未消费任务恢复和运行中 Worker 重连验证。
- Cracked 解析与适配器测试、PwnedForums 列表/详情/时间/Cookie 透传、Cookie API、Python 编译、PowerShell 解析和前端生产构建通过。
- 发布候选树不包含测试脚本、数据库、日志、运行数据、Cookie 或凭据文件。

## v0.22.0 - 2026-08-17

发布分支：`v0.22.0`

### 数据库与镜像一体化迁移

- 新增管理员“数据迁移”页面，支持上传由外部工具生成的 `.dwti` ZIP64 迁移包，并显示上传、预检、数据库导入、镜像释放和联合校验进度。
- 迁移包把 SQLite 表数据和证据镜像统一封装；导入前检查文件数量、路径穿越、符号链接、重复路径、压缩炸弹、容量限制和全部载荷 SHA-256。
- 每个迁移批次使用独立 `dwti_<批次编号>` PostgreSQL Schema 和镜像目录，逐表核对行数、空值计数、XOR256 与 SUM256；任一不一致都中止导入且不影响当前活动版本。
- 校验完成后需要管理员再次确认才写入 `active-release.json` 并重启；数据库、迁移版本或 API 健康检查失败时自动恢复上一活动版本。
- 平台会话、Cookie、令牌和凭据文件不会进入迁移包；当前仍是完整版本替换，不合并多个迁移包，也不执行跨包业务去重。
- Windows 迁移工具支持直接读取 `\\wsl.localhost` SQLite：先稳定复制数据库及 WAL/SHM 侧文件再创建快照；数据库中的镜像绝对路径会转换为可移植路径，校验包内对应文件后在 PostgreSQL 导入时重写到新镜像根目录。
- WSL 代码/文库镜像存在 Windows 保留设备名、非法字符、末尾点或空格、Unicode 等价名、超长组件及大小写冲突时，迁移包会生成稳定的 Windows 兼容归档名并同步数据库引用；无法通过显式路径安全重写的核心镜像仍会阻断打包。
- SQLite 复合索引和单列索引中的 `ASC` / `DESC` 顺序会原样创建到 PostgreSQL；Windows 导入会在释放镜像前检查最终运行路径长度，目录过深时给出明确提示，并能清理包含长路径文件的失败批次。

### PostgreSQL 运行与兼容

- 新增 SQLite / PostgreSQL 运行时选择层和 SQL 兼容适配，活动版本可同时切换数据库 Schema 与镜像根目录。
- 修复 DragonForce、Lynx 受害者列表与详情排序把日期文本和整数 ID 混入同一 `COALESCE` 导致的 PostgreSQL 500。
- Windows 首次启动会自动安装或复用 PostgreSQL 16，创建项目专用数据库和最小权限账号，并使用 DPAPI 保存本机恢复配置；已有活动版本不会被覆盖或重置密码。
- Debian / Ubuntu / WSL / Codespaces 首次启动会从校验过官方签名指纹的 PGDG 仓库安装 PostgreSQL 16；发行版退出主仓库后自动切换至 PostgreSQL 官方归档，幂等创建项目数据库与角色，并以目录 `700`、配置文件 `600` 保存当前用户私有口令。
- 修复 `winget` 已完成安装但返回非零状态时的误判，以及 Windows PowerShell 将 PostgreSQL `NOTICE` 当成终止错误的问题；安装结果只记录脱敏状态。
- WSL / Linux 启动脚本支持读取活动 PostgreSQL 版本，停止对已激活版本执行 SQLite 运行库同步。
- 迁移激活控制器会在重启前清除旧数据库和镜像目录环境覆盖，确保 PostgreSQL 活动版本及新镜像根目录真正生效。
- 修复规范化情报刷新时论坛标题未纳入聚合分组导致的 PostgreSQL 后台预热失败。
- 历史论坛记录中的非法月份、日期或分钟会按未解析时间安全降级，不再中断整批规范化刷新。
- 实体上下文合并会把缺失的国家和行业字段视为空值并安全传播有效信息，不再因旧记录缺键中断刷新。
- 规范化刷新会为 `dragonforceblog` 等未继续配置的历史来源使用迁移镜像根目录下的安全同名目录，避免重建后丢失 HTML、JSON 与截图关联。
- 论坛镜像重建改用规范文件名精确查找，并继承上一规范化版本中已验证的资源链接；不再因缺少单个截图对近万条记录反复执行目录模糊扫描。

### 大数据页面加载

- 勒索与数据泄露页面各按需返回最近 500 条；全域检索不再一次下载并创建上万个 DOM 节点。
- 全域检索进一步改为完整历史库的服务端分页：前端每页仅渲染 20 条，但总数、企业关键词、情报类型、时间或风险排序及任意页跳转都针对数据库全部记录执行，不再受前 500 条上限影响。
- 首屏列表直接在数据库中完成类型过滤、关键词检索、排序和限量读取，不再为列表请求加载全表或执行监测规则计算；全量分析保留在原有统计接口中。
- 修复全域检索 URL 关键词变量与 DOM 查询函数重名导致页面停留在“正在加载真实数据”的前端错误。
- `/api/events/search` 支持页码、每页数量、全库关键词、情报类型和排序参数；详情接口保持 `/api/events/{id}`，全部 API 大响应启用 gzip，镜像静态文件不参与压缩。

### 前端与镜像适配

- 站点管理新增“站点运行 / 平台接入”顶部切换，将 GitHub App、超级鹰、长安自动登录、代码平台会话、网盘来源、文库来源和文库会话完整迁入站点管理；监测配置保留监测对象、敏感规则与通知通道。
- 数据迁移页面改用统一的玄鉴侧栏、顶部栏、状态卡片、按钮和移动端布局，并正确高亮“数据迁移”导航项。
- 镜像预览改为保持原始比例，桌面端按视口限制最大高度，窄屏使用可用宽度且不产生横向溢出。
- PostgreSQL 活动版本会在运行环境页面明确显示数据库类型和 Schema。

### PanSou 移除

- 删除 Windows 本地源码克隆与 Go 构建、WSL Docker 容器、健康检查、环境变量注册和服务状态展示，不再随项目安装或启动 PanSou。
- 从网盘发现源、平台配置、规范化来源标签和前端接入方式中移除 PanSou；其他公开网盘来源及可选 PanHub API 不受影响。
- 升级过程不会自动删除数据库中既有的历史命中记录，只停止后续 PanSou 查询与新数据写入。

### 验证

- 增加迁移包、迁移 API、PostgreSQL SQL 兼容、DragonForce / Lynx 事件链路、Windows PostgreSQL 安装和前端布局回归测试。
- 已验证迁移包离线结构与整包 SHA-256、真实 PostgreSQL 临时表查询、Windows PostgreSQL 安装权限及前端生产构建。

## v0.21.0 - 2026-08-10

发布分支：`v.0.21.0`

### 验证码服务与会话守护

- 新增可复用的超级鹰验证码服务配置，支持通过前端或环境变量配置，凭据只保存在本机运行目录且不会回显到前端响应。
- 内置登录浏览器支持识别图片验证码并填入结果；识别错误后可调用超级鹰报错接口返分。
- 长安不夜城支持独立保存站点账号、执行双层验证码登录、验证登录后接口并更新浏览器会话；多个进程通过互斥锁避免同时登录。
- 调度器每分钟检查会话状态，即使采集任务尚未到期，只要会话失效或超过到期时间就主动恢复；失败后冷却 5 分钟再试，避免重复消耗验证码积分。
- 长安 API 遇到认证失效时会在自动恢复成功后重试一次原请求；前端增加超级鹰公共配置和长安自动登录配置卡片。

## v0.20.0 - 2026-08-04

发布分支：`v.0.20.0`

### 情报检索性能优化

- 将新版界面的内部链接切换为 Vue 路由，避免进入情报检索或详情页时触发整页刷新。
- 缓存已完成渲染的情报检索页面，返回列表时直接恢复数据、筛选条件和页面状态，不再重复构建全量结果。
- 情报事件接口优先返回已有缓存并在后台刷新，减少版块重复进入时的等待。

### 新版界面与数据适配

- 将威胁总览、勒索情报、数据泄露、漏洞预警、网盘监测、文库监测、代码监测和采集控制统一接入新版界面，保留新版 UI 已定义的页面结构与交互。
- 完善趋势图、分布统计、世界地图、进度条、站点图标、筛选条件、列表分页及详情页返回状态，修复长文本、镜像和表格在不同尺寸下的溢出与错位。
- 增加监测对象联动筛选、代码线索筛选与公开身份清理，避免占位数据、内部字段和冗余摘要进入展示层。

### Tor 网桥与采集运行

- Windows、Linux 和 WSL 启动脚本支持项目私有 Tor Expert Bundle 的自动安装、校验与每日更新，不再要求主机预装 Tor Browser。
- 完善 meek、obfs4、snowflake 等内置网桥配置、SOCKS 端口占用识别、旧进程清理、连接进度和出口状态反馈。
- 新增站点每日自动可用性探测、连接状态持久化和采集代理复用；修复站点任务异常回收后被误计为真实失败的问题。
- 增强浏览器 Worker、远程浏览器会话和流式画面控制，支持平台登录会话保存、恢复及 Windows / Linux 的浏览器回退策略。

### 情报采集与证据镜像

- 修正 DragonForce、Chaos、Lynx 和 DarkForums 等来源的入口、详情解析、受害者识别及规范化同步逻辑。
- 改进页面加载完成判定、镜像截图尺寸、HTML 证据回填和详情资源校验，避免加载中页面过早截图及无效大尺寸镜像。
- 长安不夜城详情镜像增加商品名称、标题、价格与主图完整性校验；未加载完成的历史镜像会重新进入采集队列，不再被误判为有效证据。
- 优化文库、网盘和代码监测的数据缓存、监测对象匹配、命中统计与详情翻译，减少重复请求和页面等待。

### 安装、更新与卸载

- Windows / WSL 启动脚本同步适配新版前端、后台服务、Tor 运行时和数据库路径。
- 增加 `darkweb uninstall keep-data` 与 `darkweb uninstall purge-data` 两种安全卸载模式，并提供预览、路径边界校验和显式确认。
- 项目版本更新为 `v0.20.0`，前端包版本同步为 `0.20.0`，在线更新目标切换到 `v.0.20.0` 分支。

### 发布安全

- 发布分支不包含测试脚本、测试夹具、运行数据库、日志、缓存、浏览器会话、Cookie、认证文件或本机 Tor 运行状态。
- 扩充本机凭证与运行产物忽略规则，并在提交前对最终暂存文件执行敏感文件名和凭证特征扫描。

## v0.12.0 - 2026-07-14

发布分支：`main`

### 长安不夜城监测源

- 接入长安不夜城商品列表和详情只读接口，将最新商品统一写入现有“数据泄露情报”。
- 复用内置浏览器完成 Tor 访问校验和人工登录，登录令牌及浏览器会话仅保存在运行目录。
- 增加登录状态校验和失效处理；未登录或令牌失效时任务标记为跳过，避免形成失败告警轰炸。
- 使用商品 ID 与内容哈希去重，每 30 分钟检查一次，单次最多读取 30 条列表并更新 15 条详情。
- 详情截图保留左侧商品图片和右侧商品信息区，不截取下方样例图片和长正文。

### Tor 网桥

- 完善 Linux / Codespace 网桥运行依赖安装和传输插件发现逻辑。
- 增加 Tor 启动阶段、Bootstrap 连接进度、错误诊断和进程恢复状态。
- 网桥连接成功后显示 Tor 出口 IP，并区分代理可用、出口检测失败和连接失败状态。

### 会话与采集控制

- 内置浏览器增加 onion 地址和 Tor SOCKS 代理支持，登录完成后可直接保存并复用平台会话。
- 站点健康状态增加显示名称、登录要求、登录状态、登录平台和提示信息。
- 单站点任务在缺少有效会话时返回 `auth_required`，不进入失败冷却或熔断。

### 版本更新

- 项目版本更新为 `v0.12.0`，前端包版本同步为 `0.12.0`。
- 在线更新默认固定比较并更新 GitHub `main` 分支，避免功能分支被误判为正式更新。

## v.0.10.0 - 2026-07-02

发布分支：`v.10.0`

### 文库监测

- 将文库监测升级为独立监测看板，整体交互和视觉靠近网盘监测。
- 增加免登录优先文库来源识别和默认扫描来源，包括人人文库、金锄头文库、MBA 智库文档、360 文库、腾讯文库、夸克文档、淘豆网、文档之家、搜弘文库等。
- 登录限制较重的平台不作为默认高频来源，避免绕登录、验证码或付费下载。
- 调整文库扫描默认深度，修复文库命中列表被网盘结果挤占的问题。
- 文库详情页改为网盘监测详情风格，并移除中间文件清单区域。

### 网盘与文档镜像

- 网盘监测和文库监测新增镜像文件保留能力。
- 网盘命中支持 HTML 镜像快照和旧数据缺失镜像时的回填。
- 文库命中支持公开 HTML、截图、快照路径等镜像线索保留。
- 详情页补充“镜像文件”入口，便于复核当时页面证据。

### 登录与账号

- `admin` 默认密码调整为 `123456`。
- 支持本地密码文件初始化，避免启动时因 `DARKWEB_AUTH_PASSWORD` 未配置导致无法登录。
- 增加修改密码接口和前端入口，后续可在系统内变更默认密码。

### 版本与发布

- 增加 `version.json`，版本标记为 `v.0.10.0`，更新分支为 `v.10.0`。
- 后端 API 版本信息更新为 `v.0.10.0`。
- 前端包版本更新为 `0.10.0`。
- 新增版本检查能力，用于读取本地版本并比对 GitHub 分支。
