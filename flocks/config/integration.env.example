# Project backend process
DARKWEB_AI_AGGREGATION_MODE=live
DARKWEB_AI_AGGREGATION_WORKFLOW_ID=threat_intel_search_pipeline
FLOCKS_BASE_URL=http://127.0.0.1:5175
FLOCKS_API_KEY=
# Alternative: configure FLOCKS_SECRET_FILE locally; do not commit its contents.
FLOCKS_API_TOKEN_SECRET_ID=server_api_token

# Flocks process (or configure exa_api_key in its local secret manager)
EXA_API_KEY=
FLOCKS_TG_MCP_URL=http://127.0.0.1:3333/mcp

# Configure the default LLM provider/model in Flocks itself.
# Telegram login and session files belong to the separate tg-search service.
