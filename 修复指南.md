# 前端数据加载问题修复指南

## 问题概述

**症状**：前端页面框架正常，但总览、勒索情报、数据泄露、威胁态势页面无数据显示，只有漏洞预警能正常加载。

**根本原因**：采集的数据未被标准化处理并写入数据库的 `normalized_intelligence_events` 表，导致API返回空数据。

---

## 快速修复（推荐）

### 方法1：使用快速修复脚本（最简单）

1. **双击运行** [`run_quick_fix.bat`](run_quick_fix.bat)

2. 等待脚本执行完成，应该看到类似输出：
   ```
   ✅ 标准化处理完成！
      生成事件数: 150
   
   事件类型分布:
     - data_leak: 80
     - ransomware: 50
     - vulnerability: 20
   ```

3. **重启API服务**（如果正在运行）

4. **刷新浏览器**中的前端页面

### 方法2：手动执行命令

在WSL终端中执行：

```bash
cd /var/anwang/bishe-codex-shujvqingxi/darkweb_collector

python3 scripts/quick_fix.py
```

---

## 完整诊断（如果快速修复无效）

### 使用诊断脚本

1. **双击运行** [`run_diagnose.bat`](run_diagnose.bat)

2. 脚本会自动检查：
   - ✅ 数据库文件是否存在
   - ✅ 各表的数据量
   - ✅ 最近的采集任务状态
   - ✅ 标准化处理状态
   - ✅ API数据生成测试

3. 根据诊断结果采取相应措施

### 手动诊断步骤

#### 步骤1：检查数据库

```bash
cd /var/anwang/bishe-codex-shujvqingxi/darkweb_collector

# 检查数据库文件
ls -lh data/collector.db

# 查看表数据
sqlite3 data/collector.db << EOF
SELECT COUNT(*) as forum_details FROM forum_details;
SELECT COUNT(*) as normalized_events FROM normalized_intelligence_events;
EOF
```

**预期结果**：
- `forum_details` > 0：说明有采集数据
- `normalized_events` = 0：说明需要执行标准化处理

#### 步骤2：执行标准化处理

```bash
cd /var/anwang/bishe-codex-shujvqingxi/darkweb_collector

python3 -c "
import sys
sys.path.insert(0, 'src')
from darkweb_collector.db import get_db_connection
from darkweb_collector.normalized_intelligence import ensure_normalized_intelligence
conn = get_db_connection()
events = ensure_normalized_intelligence(conn, force=True)
conn.close()
print(f'生成 {len(events)} 个标准化事件')
"
```

#### 步骤3：验证API数据

```bash
# 测试API端点
curl http://127.0.0.1:8000/api/intelligence | python3 -m json.tool | grep -A 2 "dataLeakEvents"
```

**预期结果**：应该看到 `dataLeakEvents` 数组有数据

---

## 常见问题处理

### 问题1：数据库中没有原始数据

**症状**：`forum_details` 和 `victims` 表都为空

**解决方案**：运行采集任务

```bash
cd /var/anwang/bishe-codex-shujvqingxi/darkweb_collector

# 采集chaos站点
python3 scripts/crawl.py run-site --site chaos --once

# 采集dragonforceblog站点
python3 scripts/crawl.py run-site --site dragonforceblog --once

# 采集lynx站点
python3 scripts/crawl.py run-site --site lynx --once
```

然后重新运行快速修复脚本。

### 问题2：标准化处理报错

**症状**：运行 `quick_fix.py` 时出现错误

**可能原因**：
1. 数据库表结构损坏
2. Python依赖缺失
3. 数据格式异常

**解决方案**：

```bash
cd /var/anwang/bishe-codex-shujvqingxi/darkweb_collector

# 修复数据库
python3 scripts/repair_collector_db.py

# 重新运行标准化
python3 scripts/quick_fix.py
```

### 问题3：API服务未启动

**症状**：curl测试API时连接失败

**解决方案**：

```bash
cd /var/anwang/bishe-codex-shujvqingxi/darkweb_collector

# 启动API服务
python3 scripts/serve_api.py
```

或使用一键启动脚本：

```bash
cd /var/anwang/bishe-codex-shujvqingxi/darkweb_collector
bash scripts/start_all_services_wsl.sh start
```

### 问题4：前端仍然无数据

**检查清单**：

1. ✅ 确认标准化事件数 > 0
   ```bash
   sqlite3 darkweb_collector/data/collector.db "SELECT COUNT(*) FROM normalized_intelligence_events;"
   ```

2. ✅ 确认API服务正在运行
   ```bash
   curl http://127.0.0.1:8000/api/health
   ```

3. ✅ 确认API返回数据
   ```bash
   curl http://127.0.0.1:8000/api/intelligence | python3 -m json.tool | head -50
   ```

4. ✅ 清除浏览器缓存并刷新页面
   - 按 `Ctrl + Shift + R` 强制刷新
   - 或按 `F12` 打开开发者工具，右键刷新按钮选择"清空缓存并硬性重新加载"

5. ✅ 检查浏览器控制台错误
   - 按 `F12` 打开开发者工具
   - 查看 Console 标签是否有错误
   - 查看 Network 标签，检查 `/api/intelligence` 请求的响应

---

## 验证修复成功

### 1. 数据库验证

```bash
cd /var/anwang/bishe-codex-shujvqingxi/darkweb_collector

sqlite3 data/collector.db << EOF
SELECT 
    (SELECT COUNT(*) FROM forum_details) as forum_details,
    (SELECT COUNT(*) FROM normalized_intelligence_events) as normalized_events;
EOF
```

**预期结果**：两个数字都应该 > 0

### 2. API验证

```bash
curl -s http://127.0.0.1:8000/api/intelligence | python3 -c "
import sys, json
data = json.load(sys.stdin)
print(f'数据泄露事件: {len(data.get(\"dataLeakEvents\", []))}')
print(f'勒索事件: {len(data.get(\"ransomwareEvents\", []))}')
print(f'漏洞事件: {len(data.get(\"vulnerabilityEvents\", []))}')
"
```

**预期结果**：至少有一个事件类型的数量 > 0

### 3. 前端验证

访问 `http://localhost:5173` 并检查：

- ✅ **总览页面**：显示摘要卡片、趋势图、模块预览
- ✅ **勒索情报**：显示事件列表和统计信息
- ✅ **数据泄露**：显示事件列表和统计信息
- ✅ **威胁态势**：显示态势分析、重点国家、行业分布
- ✅ **漏洞预警**：显示漏洞列表（应该已经正常）

---

## 预防措施

### 1. 确保API启动时执行warmup

检查环境变量：

```bash
echo $DARKWEB_SKIP_API_WARMUP
```

如果输出为 `1`，需要取消：

```bash
unset DARKWEB_SKIP_API_WARMUP
```

### 2. 使用统一启动脚本

始终使用 [`start_all_services_wsl.sh`](darkweb_collector/scripts/start_all_services_wsl.sh) 启动服务：

```bash
cd /var/anwang/bishe-codex-shujvqingxi/darkweb_collector
bash scripts/start_all_services_wsl.sh start
```

这会确保：
- 数据库路径正确
- API warmup执行
- 所有服务使用相同配置

### 3. 定期执行标准化处理

如果采集和API是分离运行的，可以添加定时任务：

```bash
# 编辑crontab
crontab -e

# 添加以下行（每30分钟执行一次）
*/30 * * * * cd /var/anwang/bishe-codex-shujvqingxi/darkweb_collector && python3 scripts/quick_fix.py >> /tmp/normalize.log 2>&1
```

---

## 工具文件说明

| 文件 | 用途 | 使用方式 |
|------|------|----------|
| [`run_quick_fix.bat`](run_quick_fix.bat) | 快速修复脚本（Windows） | 双击运行 |
| [`run_diagnose.bat`](run_diagnose.bat) | 完整诊断脚本（Windows） | 双击运行 |
| [`darkweb_collector/scripts/quick_fix.py`](darkweb_collector/scripts/quick_fix.py) | 快速修复（Python） | `python3 scripts/quick_fix.py` |
| [`darkweb_collector/scripts/diagnose_and_fix.py`](darkweb_collector/scripts/diagnose_and_fix.py) | 完整诊断（Python） | `python3 scripts/diagnose_and_fix.py` |
| [`plans/前端数据加载问题排查方案.md`](plans/前端数据加载问题排查方案.md) | 详细技术文档 | 阅读参考 |

---

## 技术原理

### 数据流转过程

```
1. 暗网采集 → output/*.json + output/*.html
2. 数据入库 → forum_details / victims 表
3. 标准化处理 → normalized_intelligence_events 表  ← 问题出在这里
4. API聚合 → /api/intelligence 端点
5. 前端展示 → Vue组件渲染
```

### 为什么漏洞预警能正常显示？

漏洞数据来自公开源（CISA KEV、NVD），存储在 `vulnerability_records` 表，不依赖暗网采集和标准化处理流程，因此不受影响。

### 标准化处理做什么？

[`normalized_intelligence.py`](darkweb_collector/src/darkweb_collector/normalized_intelligence.py) 的 `ensure_normalized_intelligence()` 函数会：

1. 从 `forum_details`、`victims` 等表读取原始数据
2. 统一事件格式（event_type、category、attacker、victim等）
3. 执行去重和合并
4. 计算风险评分
5. 补充国家、行业等信息
6. 写入 `normalized_intelligence_events` 表

API从这个表读取数据返回给前端。

---

## 获取帮助

如果以上方法都无法解决问题，请提供以下信息：

1. 诊断脚本的完整输出
2. API服务的启动日志
3. 浏览器控制台的错误信息
4. 数据库表的记录数统计

```bash
# 收集诊断信息
cd /var/anwang/bishe-codex-shujvqingxi/darkweb_collector

echo "=== 数据库统计 ===" > diagnostic.txt
sqlite3 data/collector.db << EOF >> diagnostic.txt
SELECT 'forum_details: ' || COUNT(*) FROM forum_details;
SELECT 'victims: ' || COUNT(*) FROM victims;
SELECT 'normalized_events: ' || COUNT(*) FROM normalized_intelligence_events;
SELECT 'crawl_jobs: ' || COUNT(*) FROM crawl_jobs;
EOF

echo "" >> diagnostic.txt
echo "=== 最近任务 ===" >> diagnostic.txt
sqlite3 data/collector.db "SELECT site_name, status, finished_at FROM crawl_jobs ORDER BY finished_at DESC LIMIT 5;" >> diagnostic.txt

cat diagnostic.txt
```
